package com.FirstSpringBoot.project.model;


public enum Role {
    ADMIN, // Represents an administrator role with elevated privileges
    USER_STANDARD   // Represents a regular user role with standard privileges
}